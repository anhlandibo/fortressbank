spring:
  application:
    name: user-service
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI}
  jpa:
    hibernate:
      ddl-auto: update

keycloak:
  auth-server-url: http://keycloak:8080
  realm: fortressbank-realm
  client-id: kong
  client-secret: XLODsjH5G3f9iqGftbrkdHeNw3NVfNdZ

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka

