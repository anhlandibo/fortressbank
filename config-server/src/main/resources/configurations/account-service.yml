server:
  port: 4001
  max-http-request-header-size: 64KB

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/fortressbank-realm
  
  data:
    redis:
      host: fortressbank-redis
      port: 6379
  
  jpa:
    hibernate:
      ddl-auto: update